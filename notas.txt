ta dando um errinho no admin, na gestao de usuarios

usar a parada que o ruan mostrou para definir a quantidade e o tipo de caracteres que o textfield aceita

botar um icone no executavel para rodar no programa:
propriedades -> clean and build -> generate javadoc